# PostgreSQL Database Credentials
POSTGRES_USER=odoo
POSTGRES_PASSWORD=odoo_super_secret_db_password

# Grafana Admin Credentials
GRAFANA_PASSWORD=grafana_admin_password

# Odoo Master Password (for database management)
# Not strictly an env var for the container, but good to keep track
ODOO_MASTER_PASSWORD=odoo_admin_master_password
